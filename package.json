{
  "name": "uilm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src",
    "sessions/*",
    "shared"
  ],
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "start": "cd src && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
